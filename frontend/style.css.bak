* { 
    margin: 0; 
    padding: 0; 
    box-sizing: border-box; 
    -webkit-tap-highlight-color: transparent; 
}

body { 
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; 
    background: #f5f5f5; 
}

.app { 
    max-width: 430px; 
    margin: 0 auto; 
    min-height: 100vh; 
    position: relative; 
}

.header { 
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); 
    color: white; 
    padding: 12px 16px 16px; 
}

.status-bar { 
    display: flex; 
    justify-content: space-between; 
    font-size: 0.75rem; 
    margin-bottom: 8px; 
}

.modo-badge { 
    background: rgba(255,255,255,0.2); 
    padding: 2px 8px; 
    border-radius: 10px; 
}

.header h1 { 
    font-size: 1.1rem; 
    margin-bottom: 2px; 
}

.fecha { 
    font-size: 0.8rem; 
    opacity: 0.9; 
    text-transform: capitalize; 
}

.fecha-selector { 
    background: white; 
    padding: 12px 16px; 
    border-bottom: 1px solid #e8e8e8; 
}

.fecha-selector label { 
    font-size: 0.7rem; 
    font-weight: 700; 
    color: #888; 
    text-transform: uppercase; 
    display: block; 
    margin-bottom: 6px; 
}

.fecha-row { 
    display: flex; 
    gap: 8px; 
}

.fecha-row input { 
    flex: 1; 
    padding: 10px; 
    border: 2px solid #e0e0e0; 
    border-radius: 10px; 
    font-size: 0.95rem; 
}

.btn-hoy { 
    padding: 10px 14px; 
    background: #667eea; 
    color: white; 
    border: none; 
    border-radius: 10px; 
    font-weight: 700; 
    cursor: pointer; 
}

.solo-lectura { 
    background: #fff3e0; 
    color: #e65100; 
    padding: 8px; 
    border-radius: 8px; 
    font-size: 0.8rem; 
    font-weight: 600; 
    text-align: center; 
    margin-top: 8px; 
    display: none; 
}

.filtros { 
    display: flex; 
    gap: 8px; 
    padding: 10px 16px; 
    background: white; 
    border-bottom: 1px solid #e8e8e8; 
}

.filtros select { 
    flex: 1; 
    padding: 10px 8px; 
    border: 2px solid #e0e0e0; 
    border-radius: 10px; 
    font-size: 0.9rem; 
}

.btn-cargar { 
    padding: 10px 14px; 
    background: #667eea; 
    color: white; 
    border: none; 
    border-radius: 10px; 
    font-weight: 600; 
    cursor: pointer; 
}

.contador { 
    display: flex; 
    justify-content: space-around; 
    padding: 10px; 
    background: white; 
    margin-top: 6px; 
    border-bottom: 1px solid #e8e8e8; 
}

.contador div { 
    text-align: center; 
}

.num { 
    font-size: 1.4rem; 
    font-weight: 700; 
    display: block; 
}

.label { 
    font-size: 0.65rem; 
    color: #888; 
    text-transform: uppercase; 
}

.presente { color: #4caf50; } 
.ausente { color: #f44336; } 
.tardanza { color: #ff9800; } 
.pendiente { color: #9e9e9e; } 
.justificado { color: #2196f3; } 
.excursion { color: #9c27b0; }

.lista { 
    padding: 8px 16px; 
    padding-bottom: 100px; 
}

.card { 
    background: white; 
    border-radius: 14px; 
    padding: 14px; 
    margin-bottom: 10px; 
    box-shadow: 0 2px 8px rgba(0,0,0,0.06); 
    opacity: 0.6; 
    transition: opacity 0.2s; 
}

.card.completo { 
    opacity: 1; 
}

.card-header { 
    display: flex; 
    justify-content: space-between; 
    margin-bottom: 10px; 
}

.card-header h3 { 
    font-size: 0.95rem; 
    font-weight: 700; 
}

.card-header span { 
    font-size: 0.78rem; 
    color: #888; 
}

.foto { 
    width: 40px; 
    height: 40px; 
    border-radius: 50%; 
    background: linear-gradient(135deg, #667eea, #764ba2); 
    color: white; 
    display: flex; 
    align-items: center; 
    justify-content: center; 
    font-weight: 700; 
    font-size: 0.9rem; 
}

.estados { 
    display: grid; 
    grid-template-columns: repeat(5, 1fr); 
    gap: 6px; 
}

.estados button { 
    padding: 10px 2px; 
    border: 2px solid #e8e8e8; 
    border-radius: 10px; 
    background: white; 
    font-size: 0.7rem; 
    font-weight: 700; 
    cursor: pointer; 
    display: flex; 
    flex-direction: column; 
    align-items: center; 
    -webkit-tap-highlight-color: transparent;
    touch-action: manipulation;
    user-select: none;
    min-height: 44px;
}

.estados button:active { 
    transform: scale(0.95); 
    transition: transform 0.1s ease;
}

.estados button.sel-p { 
    background: #4caf50; 
    color: white; 
    border-color: #4caf50; 
}

.estados button.sel-a { 
    background: #f44336; 
    color: white; 
    border-color: #f44336; 
}

.estados button.sel-t { 
    background: #ff9800; 
    color: white; 
    border-color: #ff9800; 
}

.estados button.sel-j { 
    background: #2196f3; 
    color: white; 
    border-color: #2196f3; 
}

.estados button.sel-e { 
    background: #9c27b0; 
    color: white; 
    border-color: #9c27b0; 
}

.estados button:disabled { 
    opacity: 0.5; 
    cursor: not-allowed; 
}

.acciones { 
    position: fixed; 
    bottom: 0; 
    left: 0; 
    right: 0; 
    background: white; 
    border-top: 1px solid #e8e8e8; 
    padding: 12px 16px 24px; 
    display: flex; 
    gap: 10px; 
    max-width: 430px; 
    margin: 0 auto; 
}

.acciones button { 
    flex: 1; 
    padding: 14px; 
    border: none; 
    border-radius: 12px; 
    font-size: 0.9rem; 
    font-weight: 700; 
    cursor: pointer; 
}

.btn-guardar { 
    background: linear-gradient(135deg, #4caf50, #45a049); 
    color: white; 
}

.btn-guardar:disabled { 
    background: #ccc; 
}

.btn-stats { 
    background: #f0f0f0; 
    color: #555; 
}

.toast { 
    position: fixed; 
    top: 60px; 
    left: 50%; 
    transform: translateX(-50%) translateY(-100px); 
    background: #1a1a1a; 
    color: white; 
    padding: 12px 24px; 
    border-radius: 25px; 
    font-weight: 600; 
    opacity: 0; 
    transition: all 0.3s; 
    z-index: 1000; 
}

.toast.show { 
    transform: translateX(-50%) translateY(0); 
    opacity: 1; 
}

.loading { 
    text-align: center; 
    padding: 40px; 
    color: #888; 
}

.error { 
    text-align: center; 
    padding: 40px; 
    color: #f44336; 
}